$('#spinner').addClass('d-none');
<% unless @result == 'fail' %>
  $('#photo-container').children(":first").replaceWith('<%= image_tag @recipe.photo.url %>')
  // SPINNER SHOULD ALSO RESIZE FOR NEW PHOTO
<% end %>
