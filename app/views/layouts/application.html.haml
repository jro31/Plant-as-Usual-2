!!!
%html
  %head
    %meta{charset: "UTF-8"}
    %meta{name: "viewport", content: "width=device-width, initial-scale=1, shrink-to-fit=no"}
    %title
      = page_title || 'Plant as Usual'
    = csrf_meta_tags
    = action_cable_meta_tag
    = stylesheet_link_tag 'application', media: 'all'
    =# stylesheet_pack_tag 'application', media: 'all' <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  %body{ data: { params_id: params.has_key?(:id) ? params[:id] : nil } }
    %div{class: component_classes(DarkMode::CLASSES[:page_container])}
      = render 'shared/navbar/navbar'
      = render 'shared/flashes'
      = yield
      :javascript
        const darkModeClasses = #{raw @dark_mode_css_classes}
    = javascript_include_tag 'application'
    = javascript_pack_tag 'application'
